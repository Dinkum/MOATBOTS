# Legacy reference

Shared Moatbots guidance now lives in `share/moatbots-team/SKILL.md` and is loaded by Hermes through each profile's native `skills.always_load` setting. Role identity lives in each profile's `SOUL.md`. This file is retained so local tooling and old checkouts have a clear pointer; the runtime does not load it.
